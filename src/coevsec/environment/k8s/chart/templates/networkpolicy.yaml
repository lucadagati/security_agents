apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-allow-in-namespace
  labels: { app: range }
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
